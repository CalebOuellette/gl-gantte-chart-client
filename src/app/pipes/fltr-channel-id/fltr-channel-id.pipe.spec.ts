import { FltrChannelIdPipe } from './fltr-channel-id.pipe';

describe('FltrChannelIdPipe', () => {
  it('create an instance', () => {
    const pipe = new FltrChannelIdPipe();
    expect(pipe).toBeTruthy();
  });
});
